# Test file for demonstrating code actions

workflow OrderProcessing(orderId: string) -> (Result):
    signal PaymentReceived():
        paid = true

    paid = false

    # This calls an undefined activity - code action should offer to add it
    activity ValidateOrder(orderId) -> validated

    # Watch block without hints - code action should suggest adding them
    await one:
        watch (paid):
            close Result{status: "completed"}
        timer (24h):
            close failed Result{status: "timeout"}

    # Old return statement - code action should suggest converting to close
    return Result{status: "done"}
